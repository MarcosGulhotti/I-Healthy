import { UserProviderData } from "../../Types";
import { Container } from "./styles";

const DoctorHeader = ({ user }: UserProviderData) => {
  return <Container></Container>;
};

export default DoctorHeader;
